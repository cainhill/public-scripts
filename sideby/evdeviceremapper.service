#
# 1. Copy this config file to user services directory:
#    cp evdeviceremapkeys.service /etc/systemd/user/
#
# 2. Enable and start this service:
#    systemctl --user daemon-reload
#    systemctl --user enable evdevremapkeys
#    systemctl --user start evdevremapkeys
# 
# 3. Check if evdevmapkeys logged any errors:
#    journalctl -u evdevremapkeys
#

[Unit]
Description=evdevremapkeys

[Service]
ExecStart=evdevremapkeys
Type=exec
Restart=always

[Install]
WantedBy=multi-user.target